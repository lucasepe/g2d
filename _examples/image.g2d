
im := loadPNG("./examples/arrows.png")
imW := width(im)
imH := height(im)

S := 512
size(S)
pencolor("#ffffff")
clear()

image(im, 0, 0)

push()
translate(S/2, S/2)
rotate(radians(10))
image(im, 0, 0)
pop()

snapshot("./examples/image.png")