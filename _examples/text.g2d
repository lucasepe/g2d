
S := 256
size(S)
pencolor("#ffffff")
clear()

pencolor("#000000")
pensize(2)

msg := "Welcome to g2D!"
msgWidth := textWidth(msg)

msgX := 50
msgY := 100

rotate(radians(10))
rect(msgX, msgY, msgWidth, 20)
stroke()
text(msg, msgX, msgY, 0, 0)

snapshot("./examples/text.png")