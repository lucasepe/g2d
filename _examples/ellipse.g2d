S := 256
size(S)
worldcoords(-S/2, S/2,-S/2, S/2)
pencolor("#ffffff")
clear()


pencolor(0, 0, 0, 0.1)

i := 0
while(i < 360) {
    push()
    rotate(radians(i))
    ellipse(0, 0, 0.06*S, 0.4*S)
    fill()
    pop()

    i = i + 15
}

snapshot("./examples/ellipse.png")